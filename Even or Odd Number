#include <stdio.h>

void checkEvenOdd(int num){

    if (num%2==0)
    printf("The number is even.");
    else
    printf("The number is odd.");

}
int main() {

    int num;
    printf("Enter the number:");
    scanf("%d", &num);

    checkEvenOdd(num);

    return 0;
}
